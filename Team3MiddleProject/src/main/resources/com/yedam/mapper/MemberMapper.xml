<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.mapper.MemberMapper">

	<insert id = "insertMember" parameterType="com.yedam.vo.MemberVO">
		insert into  tbl_member (member_no, member_name, member_id, 
								member_pwd, member_phone, member_email,
								member_birth,member_address, member_gender)
			values            (#{memberNo}, #{memberName}, #{memberId},
							   #{memberPwd), #{memberPhone}, #{membereEmail},
							   #{memberBirth},#{memberAddress}, #{memberGender})
	</insert>
	
	<select id="selectMember" parameterType="string" resultType="member">
		select *
		from tbl_member
		where member_id = #{id}
		and member_pwd = #{pw}
	</select>
</mapper>
