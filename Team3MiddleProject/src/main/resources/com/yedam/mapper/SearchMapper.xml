<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.SearchMapper">

  <!-- 검색 쿼리 -->
  <select id="searchProducts">
    SELECT *
    FROM tbl_product
    WHERE 1=1
      <if test="productName != null and searchTerm != ''">
        AND (productName LIKE '%' || #{productName} || '%' 
         OR description LIKE '%' || #{searchTerm} || '%')
      </if>
      <if test="maincategory != null">
        AND maincategory = #{maincategory}
      </if>
    ORDER BY created_at DESC
    OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
  </select>

  <!-- 전체 목록 조회 -->
  <select id="findAll">
    SELECT * FROM tbl_product ORDER BY created_at DESC
  </select>

</mapper>